<!DOCTYPE html>
<html>
  <head>
    <title>Tic Rack Toe</title>
    <link rel="stylesheet" href="/css/pure-min.css">
    <link rel="stylesheet" href="/css/tictactoe.css">
  </head>

  <body>
    <header>
      <h1><a href="/">Tic Rack Toe</a></h1>
    </header>

    <main>
    <form class="pure-form" method="post" action="/game/new">
      <fieldset>
        <legend>Board Size</legend>

        <% board_sizes.each do |key, description| %>
        <label class="pure-radio">
          <input type="radio" name="board_size" value="<%= key.to_s %>" checked/>
          <%= description %>
        </label>
        <% end %>
      </fieldset>

      <fieldset>
        <legend>Game Mode</legend>

        <% game_modes.each do |key, description| %>
        <label class="pure-radio">
          <input type="radio" name="game_mode" value="<%= key.to_s %>" checked/>
          <%= description %>
        </label>
        <% end %>
      </fieldset>

      <button type="submit" class="pure-button pure-input-1 pure-button-primary">Play!</button>
    </form>
    </main>
  </body>
</html>
